<?xml version="1.0" encoding="UTF-8"?>

<configuration>

	<appender name="dal"
		class="ch.qos.logback.core.rolling.RollingFileAppender">
		<file>/var/log/proptiger/dal/dal.log</file>
		<rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
			<!-- daily rollover -->
			<fileNamePattern>/var/log/proptiger/dal/dal-%d{yyyy-MM-dd}.log.gz</fileNamePattern>
			<!-- keep 30 days' worth of history -->
			<maxHistory>30</maxHistory>
		</rollingPolicy>
		<append>true</append>
		<encoder>
			<pattern>[%d{HH:mm:ss.SSS} %thread %-5level %C:%L] %msg%n</pattern>
		</encoder>
	</appender>

	<appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
		<encoder>
			<pattern>[%d{HH:mm:ss.SSS} %thread %-5level %C:%L] %msg%n</pattern>
		</encoder>
	</appender>

	<appender name="EMAIL" class="ch.qos.logback.classic.net.SMTPAppender">
		<to></to>
		<from>dal-errors@proptiger.com</from>
		<subject>DAL ERROR: %logger{20} - %m</subject>
		<layout class="ch.qos.logback.classic.html.HTMLLayout" />
	</appender>

	<root level="INFO">
		<appender-ref ref="dal" />
		<appender-ref ref="STDOUT" />
		<appender-ref ref="EMAIL" />
	</root>

</configuration>