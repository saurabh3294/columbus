package com.proptiger.data.enums.security;

/**
 * User role enum, should be handled through user database once we put that in
 * db.
 * 
 * @author Rajeev Pandey
 *
 */
public enum UserRole {

    USER, PRE_AUTH_USER, ADMIN_BACKEND
}
